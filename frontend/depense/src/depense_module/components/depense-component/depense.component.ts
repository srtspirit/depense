import {Component} from "@angular/core";
import {template} from "@angular/core/src/render3";

@Component({
  selector: 'app-depense',
  template: `depense component
      <router-outlet></router-outlet>`
})
export class DepenseComponent {
}
